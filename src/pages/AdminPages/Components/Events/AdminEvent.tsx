import { NavLink } from "react-router-dom";

function EventAdmin() {
    return ( <>
    
            this is eventAdmin page

            <NavLink to={'add-event'} >ADD ECVENT</NavLink>

    </> );
}

export default EventAdmin;